server:
  port: 9099

spring:
  application:
    name: CONFIGSERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/neeleshpatel/microservices-config.git
          clone-on-start: true
          default-label: main  # or 'master' if your repo uses that

eureka:
  instance:
    prefer-ip-address: true

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
